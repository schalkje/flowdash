## 1. Internal render-complete hook (foundational)

- [ ] 1.1 Add a handler-registry on `Dashboard` (e.g. `this._eventHandlers = new Map()` keyed by event name, with `Set<handler>` values). Internal-only at this point.
- [ ] 1.2 Add `_emit(eventName)` on `Dashboard`. Per D12 in design.md: (a) snapshot the handler set at emit start (`const handlers = [...(this._eventHandlers.get(eventName) ?? [])]`) so that handler mutations during emit affect only future emits; (b) iterate the snapshot in registration order, wrapping each invocation in `try { h(); } catch (err) { console.error('flowdash: render handler threw:', err); }` — `console.error` is intentional (not `_debugLog`) because handler errors are caller-code bugs that must surface in production.
- [ ] 1.3 Wire `this._emit('render')` into `Dashboard.onMainDisplayChange`'s rAF callback **after** `_displayChangeScheduled = false` (i.e., immediately after `dashboard.js:2067`, not before it). Per D10a in design.md, the emit MUST happen after the flag is cleared so that re-entrant state mutations from handlers get their own fresh rAF rather than being coalesced into the in-flight pass. Add a one-line comment at the emit site noting that this is the public `render` event contract for issue #14, and that the post-clear ordering is intentional (re-entrancy safety).
- [ ] 1.4 Wire a **guaranteed init-end emit**: add `this._emit('render')` at the end of `Dashboard.initialize()`, immediately after the `data-flowdash-ready="true"` attribute write (`dashboard.js:815`). This is required because `onMainDisplayChange` does not fire reliably during init for the default `zoomToRoot: false` non-prerender path (see D9 in design.md for the path matrix). The emit is direct (not routed through `onMainDisplayChange`) to bypass `_suspendDisplayChange` and rAF coalescing. Idempotency: handlers may receive two emits on the prerender path (one via `applyDeferredStatusRules` → `onMainDisplayChange`, one at init end) — the spec already requires handlers to tolerate this.
- [ ] 1.5 Add `_hasEmittedSinceInit` state on `Dashboard` (initialize to `false` in the constructor). Set it to `true` inside `_emit('render')` so it covers all emit paths (prerender, init-end, per-flush). Used by `afterRender()` for idle-resolve detection per D10b.
- [ ] 1.6 **Implementer note (future risk):** if `dashboard/js/simulation.js` (force-directed layout, currently inactive / out of scope) is reactivated, revisit whether per-tick `resizeBoundingContainer` cascades cause `render` to fire too frequently during a converging simulation. If yes, gate the per-flush emit on a "simulation is in flight" flag and emit additionally on `simulation.end`. No action required now — recorded so it shows up in code review when simulation comes back.

## 2. Public render-complete API (must-have)

- [ ] 2.1 Add `Dashboard.prototype.on(eventName, handler)` and `Dashboard.prototype.off(eventName, handler)`. `eventName` MUST be `"render"`; other event names are ignored silently (a future expansion can add more event types without breaking this contract).
- [ ] 2.2 Add `Dashboard.prototype.once(eventName, handler)` as a wrapper that registers a handler which removes itself before invoking the caller's handler.
- [ ] 2.3 Add `Dashboard.prototype.afterRender()` with the D10b idle-resolve semantics: if `this._hasEmittedSinceInit && !this._displayChangeScheduled`, return `Promise.resolve()` (microtask resolve, no synthetic emit); otherwise return `new Promise(resolve => this.once('render', resolve))`.
- [ ] 2.4 Add Playwright spec `tests/dashboard-render-event.spec.js` covering: (a) init-end guarantee — handler registered before `init` is invoked at least once before any state change, across all three paths (default non-prerender + `zoomToRoot: false`, non-prerender + `zoomToRoot: true`, prerender); (b) `afterRender()` resolves on a static dashboard post-init within one microtask AND does NOT invoke other `on('render')` handlers (no phantom emit); (c) `once` fires exactly once; (d) `off` by reference removes the listener; (e) per-flush coalescing — N `handleDisplayChange` bubbles within one frame produce exactly one emit; (f) re-entrancy — a handler that calls a mutating API (e.g., `setStatusToNodeById` or `addNode`) triggers a fresh rAF and a subsequent emit, not coalescing into the in-flight pass; (g) `afterRender()` called from inside a handler (without triggering state change) resolves in a microtask without invoking other handlers; (h) handler-throws — a throwing middle handler does NOT prevent earlier/later handlers from firing, the error is logged via `console.error` with `flowdash:` prefix, subsequent emits continue to invoke all handlers; (i) snapshot iteration — a handler that `on`s a new handler or `off`s a still-pending handler does not affect the current emit, only future emits; (j) handler survival across `setData` — a handler registered before `setData` is still invoked on the post-`setData` render emit.

## 3. getNodeBounds (must-have)

- [ ] 3.1 Implement `Dashboard.prototype.getNodeBounds(id)`. Resolve the node via `this.main.root.getNode(id)`; return `null` for unknown id. For matched nodes, return `getBoundingBoxRelativeToParent(node.element, this.main.container)` (or an equivalent zone-system read producing the same coordinate frame). Return `null` if the node has no DOM element (detached due to collapsed ancestor) — detect via `!node.element || !node.element.node()`, matching the guard already in `getBoundingBoxRelativeToParent`.
- [ ] 3.2 Verify round-trip: feeding the returned bbox into the math `zoomToBoundingBox` uses must yield the same target as the existing `zoomToNodeById` for that node. Add a unit-style assertion in the Playwright spec.
- [ ] 3.3 Add Playwright spec `tests/dashboard-get-node-bounds.spec.js` covering: rendered node returns bounds; collapsed-ancestor returns null; unknown id returns null; **removed-id returns null** — add a node via `addNode`, confirm `getNodeBounds` returns bounds, call `removeNode`, confirm `getNodeBounds` returns null with the same shape as the unknown-id case; bounds invariant under `transform: scale(2)` on host SVG; first-match semantics on duplicate-id-on-load data (use a test fixture with a deliberate duplicate id and assert behavior matches `getNode(id)`).

## 4. panToBounds (must-have)

- [ ] 4.1 In `zoomManager.js`, extract the outer-bounds clamp currently embedded in `zoomToBoundingBox` / `computeFit` into a reusable helper (e.g. `clampPanTransform(transform)` or `computePanOnly(bbox, currentK, padding)`). Refactor `zoomToBoundingBox` to call it. This is pure refactor; assert no behavior change with a regression run of the existing zoom Playwright specs.
- [ ] 4.2 Implement `Dashboard.prototype.panToBounds(bbox, { animate = true, padding = 0 } = {})`. Compose: compute the pan-only transform at the current zoom level `main.transform.k`; clamp via 4.1's helper; apply via `zoomManager.applyTransform(transform, { animate, duration: <pan tween duration> })`.
- [ ] 4.3 Handle oversized bbox (bbox + padding larger than viewport at current zoom): center on bbox center, zoom unchanged. Implement in the same compute step as 4.2.
- [ ] 4.4 Return a Promise that resolves when the transform application completes. For `animate: true`, resolve on transition end; for `animate: false`, resolve in a microtask after the synchronous apply.
- [ ] 4.5 Add Playwright spec `tests/dashboard-pan-to-bounds.spec.js` covering: pan to off-screen bbox brings it into view with zoom unchanged; in-view bbox is a no-op (transform unchanged); clamping at the diagram edge prevents whitespace; oversized bbox centers without zoom change; `padding` is applied uniformly; `animate: false` resolves synchronously.

## 5. revealNode (must-have)

- [ ] 5.1 Add an internal helper that, given a node, walks up to the root collecting the chain of currently-collapsed ancestor containers in outermost-to-innermost order.
- [ ] 5.2 Implement `Dashboard.prototype.revealNode(id)`. Resolve via `getNode(id)`; reject with a descriptive error if not found. Otherwise iterate the collapsed-ancestor chain and invoke the same expand operation each ancestor would receive from a user-click on its toggle. Rely on existing status-cascade and auto-collapse settings; do not bypass them.
- [ ] 5.3 Compose with `afterRender()` so the returned Promise resolves after the final flush triggered by the expand cascade. If no ancestors were collapsed, still await one flush for consistency (use a microtask Promise that resolves on next flush or immediately if no flush is pending).
- [ ] 5.4 Add Playwright spec `tests/dashboard-reveal-node.spec.js` covering: single collapsed ancestor expanded and `getNodeBounds` non-null afterward; nested collapsed ancestors both expanded; already-visible node resolves with no state change; unknown id rejects; **removed-id rejects with the same error shape as unknown-id** (add, remove, then call `revealNode` and assert rejection); expanded ancestors stay expanded on subsequent user interaction.

## 6. getDatasetNodeIds (must-have, new — enables the multi-placement use case)

- [ ] 6.1 Implement `Dashboard.prototype.getDatasetNodeIds(datasetId)` as a thin wrapper over `this.main.root.getNodesByDatasetId(datasetId)`, mapping to `node.id` and returning `string[]`. Return `[]` (never null) for unknown datasetId. Preserve tree-walk order from the underlying call.
- [ ] 6.2 Add Playwright spec `tests/dashboard-get-dataset-node-ids.spec.js` covering: multiple placements return all ids in tree-walk order; single placement returns one id; unknown datasetId returns `[]`; each returned id is accepted by `getNodeBounds`, `revealNode`, and `setNodeClass` without throwing.

## 7. data-dataset-id DOM attribute (optional)

- [ ] 7.1 In `nodeBase.js` at the same point as the existing `.attr('id', this.id)` (line 311), conditionally set `data-dataset-id` from `this.data?.datasetId` when that value is a non-empty string. Preserve the `id` attribute unchanged.
- [ ] 7.2 Ensure the attribute is updated if `data.datasetId` is mutated and removed when the node is removed from the DOM. Audit the node-update path to confirm where attribute writes are re-applied (likely in the same render method or its near vicinity).
- [ ] 7.3 Add Playwright spec `tests/dashboard-data-dataset-id.spec.js` covering: node with datasetId has both `id` and `data-dataset-id`; all placements sharing a datasetId carry the attribute; node without datasetId has only `id` (no `data-dataset-id`); `querySelectorAll('[data-dataset-id="X"]')` returns all placements in tree-walk order.

## 8. setNodeClass (optional)

- [ ] 8.1 Implement `Dashboard.prototype.setNodeClass(id, className, enabled)`. Resolve via `getNode(id)`; silent no-op for unknown id or for a node with no rendered DOM element. Toggle the class on `node.element.node()` via `classList.add` / `classList.remove`.
- [ ] 8.2 Add Playwright spec `tests/dashboard-set-node-class.spec.js` covering: add a class; remove a class; multi-placement fan-out by iterating `getDatasetNodeIds`; unknown id is a silent no-op; hidden placement (detached `<g>`) is a silent no-op.

## 9. Documentation

- [ ] 9.1 Add a "Public API hooks" section to `dashboard/documentation/dashboard.md` documenting all additions. Include: the `id` vs `datasetId` distinction (calling out that `id` is unique by design and `datasetId` is the non-unique handle); the coordinate-frame contract (D2 in design.md); the leak risk for never-deregistered `on('render')` handlers (D3 / Risks); a worked example using `getDatasetNodeIds` to iterate placements; the `setData` re-init lifecycle (D6 / setData scenarios — handlers survive setData; the `await setData(d); await afterRender();` pattern; cached ids may become stale per D11); the theme-switching contract (theme switches are CSS-only — they do not fire `render` and caller classes persist; callers wanting to react to theme changes should listen for `window.addEventListener('flowdash:themechange', …)` per the existing `themeManager`).
- [ ] 9.2 Cross-link from `dashboard/documentation/zone-system.md` to the coordinate-space note in `dashboard.md` (no content change to zone-system.md itself).
- [ ] 9.3 Add a one-line entry in `dashboard/CHANGELOG.md` (or equivalent) referencing issue #14 and noting the dropped `data-node-id` / `placementIndex` versus what the issue originally proposed (since downstream readers may have read the issue first).

## 10. Release

- [ ] 10.1 From `/dashboard/`, run `npm version minor` to bump to `1.5.0`. Verify the bundle banner (BannerPlugin) picks up the new version on `npm run build`.
- [ ] 10.2 Run the full Playwright suite (`npm test` from repo root) and confirm no regressions against the documented baseline. Address any failures introduced by the new spec files.
- [ ] 10.3 Update issue #14 with a comment linking to the merged PRs and noting the release version.
